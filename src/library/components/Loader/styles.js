import {
    StyleSheet
  } from 'react-native';

const styles = StyleSheet.create({
    container:{
        width: "100%",
        height: 50,
        backgroundColor: "#d1d7dc",
        alignItems: "center",
        justifyContent: "center",
        opacity: .5,
        position: "absolute",
        bottom: 0
    }
  });

export default styles